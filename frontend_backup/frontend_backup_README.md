# frontend_backup/ — DEPRECATED

This directory is a backup snapshot from an earlier iteration. **It is not used
by the live Vercel deployment** and contains stale contract addresses.

The live app is built from `frontend/` (see `DEPLOY_WEB.md`). The current
contract address is set in `frontend/src/config/contract.ts`.

Do not edit anything here expecting it to affect the running site.
